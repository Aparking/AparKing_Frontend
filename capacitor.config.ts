import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sergiosantiago.aparking',
  appName: 'AparKing_Frontend',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
